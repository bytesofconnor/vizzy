export function HowToUse() {
  return (
    <section
      id="make-one"
      style={{
        marginTop: 22,
        maxWidth: 540,
      }}
    >
      <p style={{ fontSize: 15, lineHeight: 1.5, margin: 0 }}>
        To make your own, you add Vizzy once inside Claude or ChatGPT — the same way you add a
        tool, not a website, not a GitHub clone. That connector is not live yet. Until it is, copy
        one of these.
      </p>
    </section>
  );
}
